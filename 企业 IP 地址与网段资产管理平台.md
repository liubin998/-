# 企业 IP 地址与网段资产管理平台

## 网络设备联动、IP 使用诊断与智能协同最终设计方案

**文档版本：** V1.0  
**文档状态：** 最终设计方案  
**适用范围：** 多分支企业网络、IPv4/IPv6、公网/内网/专线/云资源、华为等交换机、深信服 AC  
**当前阶段：** 方案设计完成，尚未进入功能开发与生产设备接入

---

## 1. 项目概述

本项目拟建设一套面向企业总部与多分支机构的 IP 地址与网段资产管理平台，用于替代目前依靠 KDocs 文档分散维护的 IP 台账。平台将组织分支、网段规划、IP 地址、设备资产、人员部门、网络设备现场信息、权限、导入来源、协同事项和操作日志统一管理。

平台的目标不是简单地把 Excel 或 KDocs 表格搬到网页中，而是建立一个可以计算、检索、校验、协作和追溯的 IPAM 系统。用户可以输入单个 IP，系统自动识别其所属 CIDR 网段；也可以按照分支、设备、负责人、部门、状态和网段查找资源；还可以通过网络设备和深信服 AC 的现场信息，判断某个 IP 是否可能正在使用、对应什么 MAC、位于哪个交换机端口，以及台账和现场是否存在不一致。

> **总体原则：结构化数据库保存事实，IP/CIDR 规则引擎负责确定性计算，网络设备提供现场观测，AI 负责理解自然语言和组织解释，但不能绕过权限、直接猜测数据或代替规则引擎。**

---

## 2. 产品定位与建设目标

平台定位为“企业级 IPAM + 网络设备联动诊断 + 协同审计平台”。它既服务于日常 IP 查询和分配，也服务于网络规划、分支运维、资产治理、冲突处理和网络安全审计。

| 建设目标 | 目标说明 |
|---|---|
| 集中管理 | 将多分支 KDocs 台账统一到一个平台中 |
| 规范建模 | 统一管理 IPv4、IPv6、CIDR、子网掩码、地址范围和资源归属 |
| 快速查询 | 支持按 IP、网段、设备、负责人、部门、分支和状态查询 |
| 智能计算 | 自动识别单 IP 所属网段、地址范围、使用率和可用数量 |
| 现场联动 | 从交换机和深信服 AC 获取 ARP、MAC、端口、在线用户等信息 |
| 状态诊断 | 区分明确使用、可能使用、未发现使用、无法判断和台账冲突 |
| 灵活扩展 | 支持管理员自定义分配对象类型和字段 |
| 安全协作 | 支持总部、分支、维护、查看和审计等角色及分支范围授权 |
| 可追溯 | 保存导入来源、现场观测、操作前后差异和协同处理过程 |
| AI 辅助 | 支持权限受控的自然语言查询和可解释结果 |

---

## 3. 总体架构

### 3.1 推荐架构

推荐采用“中心管理平台 + 分支网络采集器 + 厂商适配器”的架构。中心平台承载网页、数据库、权限、IPAM 规则、AI 查询和审计；每个分支部署一个轻量级采集器，负责访问本地交换机和深信服 AC，并向中心平台主动上报标准化结果。

```text
┌──────────────────────────────────────────┐
│              IPAM 中心管理平台             │
│ Web UI │ tRPC/API │ 权限 │ 审计 │ AI 查询  │
│ IP/CIDR 规则引擎 │ 导入中心 │ 仪表盘        │
└──────────────────┬───────────────────────┘
                   │ HTTPS 主动出站连接
          ┌────────▼────────┐
          │   分支网络采集器  │
          │ 任务调度/缓存/重试 │
          │ 协议适配/数据标准化 │
          └──────┬───────┬───┘
                 │       │
        ┌────────▼──┐ ┌──▼─────────┐
        │ 华为等交换机 │ │ 深信服 AC   │
        │ SNMPv3      │ │ RESTful API │
        │ NETCONF     │ │ 在线用户    │
        │ RESTCONF    │ │ IP/MAC      │
        │ SSH 只读兜底 │ │ 状态接口    │
        └─────────────┘ └─────────────┘
```

华为官方资料显示，NETCONF 使用 XML、RPC 和 YANG 数据模型进行网络管理，通常通过 SSH 建立安全会话；RESTCONF 基于 HTTP，为设备资源提供 REST 风格的查询和操作接口，并支持 XML 或 JSON 数据格式。[1] [2] 因此平台应优先采用结构化协议，只有设备型号或软件版本不支持时才使用版本化的 SSH 只读解析器。

### 3.2 部署选项

| 方案 | 适用场景 | 优点 | 风险与限制 | 结论 |
|---|---|---|---|---|
| 中心平台直连设备 | 中心与所有分支管理网已通过专线/VPN 互通 | 组件少、部署简单 | 需要放通管理网，中心暴露面较大 | 可作为特定环境方案 |
| 中心平台 + 分支采集器 | 分支管理网隔离，设备只允许本地访问 | 安全边界清晰，适配多分支和多厂商 | 需要维护采集器 | **推荐方案** |
| 全部部署企业内网 | 数据不能离开企业或必须本地化 | 网络访问直接，数据边界清晰 | 部署、升级和高可用由企业负责 | 合规场景可选 |

采集器只主动连接中心平台，中心平台不需要直接向公网暴露交换机或 AC 管理端口。采集器可部署在企业内网虚拟机、物理机或容器环境中，具体形态由企业现有运维体系决定。

---

## 4. 功能模块设计

| 模块 | 主要功能 |
|---|---|
| 总览仪表盘 | 分支数、网段数、地址使用率、空闲量、冲突、设备健康和近期动态 |
| IP 快速查询 | 输入 IP、CIDR、设备、负责人或部门进行统一搜索 |
| 高级检索 | 多条件组合筛选，支持保存常用查询条件 |
| 网段管理 | 创建、编辑、拆分、嵌套规划、冲突识别和使用率统计 |
| IP 地址台账 | 查看地址状态、分配对象、现场观测、历史记录和冲突 |
| 资产与对象 | 管理电脑、服务器、摄像头、网络设备、虚拟机、人员和部门等 |
| 网络设备 | 管理交换机、AC、路由、防火墙、采集器和设备能力 |
| 联动诊断 | 查看 IP 存活、IP/MAC、ARP、端口、在线用户和台账一致性 |
| AI 查询助手 | 用自然语言查询授权范围内的 IPAM 和现场诊断结果 |
| 导入中心 | 导入 KDocs 导出的 Excel/CSV，字段映射、预检、确认和追溯 |
| 冲突处理 | 处理重复 IP、重叠网段、现场与台账不一致等问题 |
| 协同审计 | 操作日志、变更差异、责任人、评论、处理状态和关闭原因 |
| 组织与权限 | 管理总部、分支、用户、角色、分支范围和字段权限 |
| 系统配置 | 自定义对象类型、字段、状态、诊断窗口和基础枚举 |

---

## 5. 核心数据模型

### 5.1 IPAM 基础实体

| 实体 | 核心字段 | 设计说明 |
|---|---|---|
| 组织分支 | 名称、编码、上级组织、负责人、状态 | 支持未来持续新增分支 |
| 用户 | 登录身份、姓名、邮箱、部门、状态 | 与系统登录身份关联 |
| 角色 | 名称、权限集合 | 角色定义与分支范围分离 |
| 分支授权 | 用户、分支、角色、有效期 | 同一用户可管理多个分支 |
| 网段 | 网络地址、前缀、掩码、协议、用途、类型、分支 | 记录地址规划 |
| IP 地址 | 标准化 IP、协议、状态、网段 | 记录单个地址的业务状态 |
| 分配对象类型 | 类型名称、字段定义、版本、启停状态 | 管理员可自定义 |
| 分配对象 | 类型、名称、编号、负责人、部门、自定义字段 | 适配不同资产类别 |
| IP 分配关系 | IP、对象、分配时间、释放时间、原因 | 保存当前和历史关系 |
| 导入批次 | 来源文件、分支、导入人、状态、统计 | 关联 KDocs 导出文件 |
| 导入错误 | 行号、原始值、错误类型、建议 | 支持错误行反馈 |
| 操作日志 | 操作人、动作、对象、前后差异、时间、分支 | 支持审计和回溯 |
| 协同事项 | 类型、责任人、状态、评论、截止时间 | 管理冲突与待处理事项 |

### 5.2 自定义分配对象

平台不应把服务器、电脑、摄像头、虚拟机、员工和部门等写死为固定类型。管理员可以创建“对象类型”，并配置对象字段。例如服务器可以包含主机名、操作系统、机房、业务系统和责任人；摄像头可以包含安装位置、区域和维护单位；专线可以包含运营商、线路编号和对端地址。

建议字段类型支持文本、数字、日期、单选、多选、布尔值、IP、URL 和备注。字段定义需要版本化，避免后续修改字段后无法解释历史记录。

---

## 6. IP、CIDR 与地址计算规则

IP 计算必须由服务端确定性规则引擎完成。AI 只能提取用户输入中的地址或网段，不能自行推导计算结论。

| 计算能力 | 示例 | 系统行为 |
|---|---|---|
| CIDR 解析 | `192.168.0.0/16` | 解析网络地址、前缀和掩码 |
| 子网掩码识别 | `255.255.0.0` | 转换为 `/16` 并校验连续性 |
| 地址范围 | `192.168.0.0/16` | 计算起止地址、网络地址和广播地址 |
| 单 IP 归属 | `192.168.1.254` | 匹配所有包含该 IP 的网段 |
| 最长前缀匹配 | `/16` 与 `/24` 同时存在 | 默认选择更具体的 `/24`，同时提示嵌套规划 |
| IPv6 标准化 | 多种等价写法 | 转成统一格式后比较和存储 |
| 可分配数量 | `/24`、IPv6 前缀 | 计算理论数量并扣除企业保留规则 |
| 网段重叠 | `/16` 与内部 `/24` | 区分合法父子规划和非法重复规划 |
| IP 冲突 | 一个 IP 多条占用记录 | 标记冲突并生成待处理事项 |

地址统计必须区分理论地址数、网络保留数、已占用数、冲突数、不可用数、可分配数和空闲数。对于 IPv4，应根据企业规则决定是否扣除网络地址、广播地址、网关地址和专用保留地址；对于 IPv6，应避免直接套用 IPv4 的广播地址规则。

查询 `192.168.1.254` 时，系统先查精确 IP 记录，再查包含该地址的所有网段。如果发现多个候选网段，系统采用最长前缀匹配，同时提示存在嵌套或重叠规划，防止隐藏网络设计问题。

---

## 7. 查询与台账状态

### 7.1 查询能力

统一搜索框支持 IP、CIDR、设备名称、负责人、部门、分支和状态。高级检索提供多条件组合，并在结果中显示命中原因、数据来源和最近更新时间。

| 查询场景 | 返回内容 |
|---|---|
| 输入单个 IP | 所属网段、分支、台账状态、现场诊断、设备和负责人 |
| 输入 CIDR | 地址范围、使用率、空闲量、子网和关联资产 |
| 输入设备 | 设备关联 IP、MAC、端口、分支和历史 |
| 输入负责人 | 名下对象、IP 数量、分支和部门 |
| 查询空闲地址 | 指定网段中排除保留和冲突后的候选地址 |
| 查询冲突 | 重复 IP、重叠网段、MAC 不一致和跨分支冲突 |

### 7.2 业务状态与现场诊断状态分离

IPAM 业务状态描述地址在台账中的管理状态；现场诊断状态描述最近一次网络观测结果。两者不能混成一个字段。

| 业务状态 | 含义 |
|---|---|
| 空闲 | 当前可分配 |
| 占用 | 已关联对象 |
| 保留 | 暂不分配但属于规划 |
| 已释放 | 曾经使用过，目前已释放 |
| 冲突 | 存在重复或归属矛盾 |
| 不可用 | 受网络或业务规则限制 |
| 待确认 | 来源数据尚未确认 |

| 现场状态 | 含义 |
|---|---|
| 明确使用 | 存在高可信度在线或网络关系证据 |
| 可能使用 | 只有辅助探测或较旧证据 |
| 未发现使用 | 在有效采集窗口内没有证据 |
| 无法判断 | 设备离线、权限不足或数据过期 |
| 台账冲突 | 现场与台账关系不一致 |
| 采集不完整 | 接口返回被截断或部分能力不可用 |

“已释放”不能自动等于“空闲”；“未发现使用”也不能在设备离线时生成。所有结论必须显示数据年龄和证据来源。

---

## 8. 网络设备接入设计

### 8.1 设备接入模型

设备统一抽象为 `NetworkDevice`，记录厂商、型号、软件版本、管理地址、分支、设备角色、接入协议、能力清单、凭据引用、启用状态和最近通信结果。数据库只保存凭据引用，不保存明文密码。

### 8.2 协议优先级

| 优先级 | 协议 | 主要用途 | 设计定位 |
|---|---|---|---|
| 1 | NETCONF/YANG | 结构化设备状态和配置数据 | 支持时优先使用 |
| 2 | RESTCONF/YANG | HTTP/HTTPS 结构化资源查询 | 适合 Web 化管理 |
| 3 | SNMPv3 | 设备状态、接口和部分表项 | 主要用于监控和观测 |
| 4 | SSH 只读命令 | 兼容暂未覆盖的型号 | 版本化解析，禁止写操作 |
| 5 | ICMP/TCP | 辅助可达性探测 | 不能单独证明 IP 已使用 |

首期只做只读采集，不通过平台自动修改交换机 VLAN、端口、路由或配置。未来若增加配置能力，需要独立的审批、变更前快照、回滚和高级审计设计。

### 8.3 华为交换机适配

华为交换机适配器应根据型号和软件版本记录具体能力，不应仅根据“华为”这一厂商名称假设能力相同。接入前应探测设备是否支持 SNMPv3、NETCONF、RESTCONF、ARP 表、MAC 地址表、端口状态、VLAN 和 LLDP 等能力。

SSH 兜底方式必须采用型号/版本对应的解析模板。解析失败时保存脱敏后的错误摘要并标记“数据不可用”，不能把空结果解释为没有设备或没有 IP 使用。

---

## 9. 深信服 AC API 专项设计

### 9.1 文档确认的接入机制

根据用户提供的《Sangfor_AC_API.pdf》，深信服 AC 提供 RESTful 服务，示例端口为 `9999`。请求方需要在 AC 的开放接口配置中启用服务，并将请求方 IP 或 IP 段加入白名单。请求需要使用共享密钥和随机数进行 MD5 校验。

认证计算规则为：

```text
random = 每次请求新生成的随机字符串
md5 = MD5(sharedSecret + random)
```

GET 请求将 `random` 和 `md5` 放在 URL 查询参数中；POST 请求使用 `application/json`，将 `random` 和 `md5` 放入 JSON 请求体。文档要求同一个 random 在 1 小时内只能使用一次，因此失败重试必须重新生成 random 和 md5。

平台应统一请求头：

```text
Accept-Language: zh-CN
```

PDF 中包含“只支持本地请求”的错误信息，因此 AC 适配器必须部署在能直接访问 AC 管理网的分支采集器中，不能假设中心公网平台可以直接调用 AC。

### 9.2 已确认接口

| 接口 | 方法 | 作用 | IPAM 用途 |
|---|---|---|---|
| `/v1/status/version` | GET | 获取 AC 版本 | 设备登记、能力识别 |
| `/v1/status/online-user` | GET | 获取在线用户数 | 健康指标，不作为单个 IP 证据 |
| `/v1/status/session-num` | GET | 获取会话数 | AC 状态监控 |
| `/v1/status/flux-units` | GET | 获取流量单位 | 仅流量指标场景调用 |
| `/v1/ipmac-bindinfo?search=VALUE` | GET | 按 IP/MAC 查询绑定 | IP/MAC 关系观测 |
| `/v1/online-users?_method=GET` | POST | 查询在线用户信息 | 在线 IP、MAC、用户和终端证据 |

在线用户接口支持 `status`、`terminal` 和 `filter` 参数。`filter.type` 可以是 `user`、`ip` 或 `mac`；当类型为 `ip` 时，值可以是 IP 地址或 IP 段数组。

在线用户返回字段包括 `name`、`show_name`、`father_path`、`ip`、`mac`、`terminal`、`authway`、`login_time` 和 `online_time`。其中 `login_time` 是 Unix 时间戳，`online_time` 单位为秒。文档说明在线用户数组最多返回 100 条记录。

### 9.3 AC 适配器职责

AC 适配器封装所有深信服特有逻辑，对 IPAM 核心输出统一的现场观测结构。

```text
SangforAcAdapter
├── testConnection()
├── getVersion()
├── getHealthStats()
├── queryOnlineUsers(filter)
├── queryIpMacBinding(search)
├── normalizeIp(value)
├── normalizeMac(value)
├── normalizeTimestamp(value)
└── mapError(code, message)
```

统一观测结果至少包括：来源设备、分支、IP、MAC、用户名、终端类型、登录时间、在线时长、证据类型、采集时间、过期时间、原始记录引用和可信度。

### 9.4 AC 接口错误处理

| AC 错误 | 平台归类 | 处理方式 |
|---|---|---|
| 只支持本地请求 | 网络范围错误 | 检查采集器是否位于 AC 本地网络 |
| Restful 服务未启用 | 配置错误 | 提示启用 AC 开放接口 |
| 请求 IP 不在白名单 | 白名单错误 | 提示加入采集器 IP 或最小必要网段 |
| 权限校验失败 | 凭据/签名错误 | 暂停高频重试，要求人工检查 |
| 未知错误、获取数据失败 | 设备或版本异常 | 保存脱敏摘要并进入排查 |
| HTTP 成功但业务 `code=1` | 业务失败 | 不得把空数据当成无在线用户 |

### 9.5 100 条返回上限

在线用户接口单次最多返回 100 个用户，平台不能用一次“全量请求”判断所有网段状态。推荐按 IPAM 授权网段进行分片查询。对于 `/16` 等大型网段，应拆成更小网段或地址片段，并记录每次查询的范围。

若目标版本提供分页参数，应以该版本接口文档为准；不能自行假设存在 `page`、`limit` 或 `offset`。如果 `count` 大于实际返回数组长度，采集运行必须标记为“结果不完整”，未返回地址不能判断为空闲。

### 9.6 AC 安全边界

首期只调用版本、状态、在线用户和 IP/MAC 查询接口。虽然 PDF 也描述了增加和删除 IP/MAC 绑定的接口，但首期不自动调用写接口。共享密钥只由服务端安全保存，前端、数据库普通字段、日志和 AI 上下文都不能出现密钥、完整签名 URL 或可重放认证参数。

---

## 10. IP 存活与使用诊断

### 10.1 证据来源

| 证据 | 主要事实 | 默认可信度 |
|---|---|---:|
| 深信服 AC 在线用户 | IP、MAC、用户、终端和在线状态 | 高 |
| 深信服 AC IP/MAC 绑定 | IP 与 MAC 绑定关系 | 中高 |
| 交换机 ARP/IPv6 邻居 | IP 与 MAC 的三层邻居关系 | 高 |
| 交换机 MAC 表 | MAC、端口、VLAN | 高 |
| DHCP 租约 | IP、MAC、租约有效期 | 高 |
| ICMP | 网络路径可达性 | 中 |
| TCP 端口探测 | 指定服务端口可达 | 中 |
| 历史台账 | 计划或登记关系 | 低到中 |

### 10.2 诊断结论

系统同时维护“可达性”和“使用证据”。Ping 不通不代表空闲，Ping 通也不代表合法占用。诊断结果必须提供结论、证据、数据来源、采集时间和可信度。

| 证据组合 | 诊断结果 |
|---|---|
| AC 在线 + ARP/MAC + 交换机端口命中 | 明确使用 |
| AC 在线但台账 MAC 不一致 | 明确使用，台账冲突 |
| 只有 IP/MAC 绑定，没有在线会话 | 可能使用或存在静态绑定 |
| 只有 ICMP/TCP 成功 | 可能使用 |
| 在有效窗口内无证据 | 未发现使用 |
| AC/交换机离线、权限不足或数据过期 | 无法判断 |
| 结果数量被接口上限截断 | 采集不完整，不能判断空闲 |

### 10.3 时间窗口

建议 AC 在线用户默认有效窗口为 5 分钟，IP/MAC 绑定和交换机 ARP/MAC 观测默认有效窗口为 10 分钟。窗口应可按设备、分支和数据类型调整。

采集时间 `observedAt` 表示采集器获得有效响应的时间；AC 的 `login_time` 只表示用户上线时间，不能替代采集时间。内部统一以 UTC 毫秒存储，页面按用户本地时间展示。

---

## 11. 同步任务与运行治理

| 任务 | 周期建议 | 内容 |
|---|---:|---|
| AC 健康检查 | 5 分钟 | 版本、在线用户数、会话数和接口响应 |
| AC 在线用户 | 1–5 分钟 | 在线 IP、MAC、用户、终端和登录信息 |
| AC IP/MAC | 5–10 分钟 | 绑定关系和描述 |
| 交换机 ARP/邻居 | 5–10 分钟 | IP、MAC、接口和邻居状态 |
| 交换机 MAC/端口 | 5–10 分钟 | MAC、端口、VLAN和端口描述 |
| DHCP 租约 | 5–15 分钟 | 地址、MAC和租约有效期 |
| 设备基础信息 | 每日或版本变更后 | 型号、版本、序列号和能力 |
| 全量校准 | 每日或每周 | 检查遗漏、过期和跨来源冲突 |

每个任务需要配置超时、并发数、重试次数和指数退避。认证错误、白名单错误和接口未启用不应无限重试；网络超时可以退避重试，但每次 AC 重试必须生成新的 random 和 md5。

采集器应支持短期本地缓存。中心平台暂时不可用时，采集器可以缓存摘要，恢复后补传。缓存必须有容量上限、过期策略和加密保护。

分钟级或小时级轮询属于后台确定性任务，应由平台自身的长期运行机制执行，不能依赖用户浏览器打开，也不应通过每次启动完整人工会话来完成。

---

## 12. 权限、凭据与审计

### 12.1 角色与分支范围

平台采用“角色权限 + 分支范围”的二维授权模型。

| 角色 | 默认能力 |
|---|---|
| 总部管理员 | 管理所有分支、用户、网段、IP、设备、导入和配置 |
| 分支管理员 | 管理被授权分支的资源、设备和导入 |
| 维护人员 | 编辑授权范围内的资产和分配关系 |
| 查看者 | 查看授权范围内的台账和诊断结果 |
| 审计人员 | 查看授权范围内日志、导入批次和差异 |

权限至少拆分为查看、创建、编辑、分配/释放、导入、冲突处理、设备管理、主动诊断、用户管理、系统配置和审计查看。删除网段、批量释放 IP、批量探测、修改凭据和注册采集器等高风险动作需要二次确认并记录原因。

### 12.2 设备凭据

交换机和 AC 应创建专用的只读账号。优先使用 SNMPv3、SSH 密钥、HTTPS 或其他安全认证方式。AC 共享密钥由服务端安全配置管理，平台只显示是否已配置和最后轮换时间。

采集器首次注册使用一次性注册码或人工审批。注册后使用短期令牌或证书与中心平台通信，并绑定到具体分支。采集器不能伪造其他分支设备身份。

### 12.3 审计内容

审计日志需要记录登录、设备新增、设备凭据变更、采集器注册、接口测试、主动诊断、导入、IP 分配、释放、修改、删除、冲突处理和 AI 查询。记录内容包括操作人、时间、分支、对象、动作、前后差异、结果、请求来源和关联批次。

日志禁止保存共享密钥、密码、Token、完整签名 URL 和完整敏感响应。高风险日志应支持只读归档和导出。

---

## 13. KDocs Excel/CSV 导入

导入来源限定为 KDocs 导出的 `.xlsx` 和 `.csv` 文件，不直接依赖 KDocs 在线接口。原始文件和导入校验结果使用安全对象存储保存，数据库保存文件元数据、导入批次和错误明细。

导入流程为：上传文件、识别工作表、字段映射、数据预检、人工确认、正式入库、结果追溯。预检结果分为可导入、警告、错误和冲突。系统不能静默覆盖现有台账。

| 导入检查 | 处理方式 |
|---|---|
| IP 格式错误 | 标记错误行，不允许入库 |
| CIDR 或掩码非法 | 标记错误并提示修正 |
| 必填字段为空 | 错误或警告，按字段规则处理 |
| 重复 IP | 标记冲突，禁止自动覆盖 |
| 网段重叠 | 区分父子规划和非法重叠 |
| 分支归属缺失 | 进入待确认 |
| 设备/负责人无法匹配 | 保留原始值，提示人工关联 |
| 同一批次重复行 | 标记批内重复 |
| 与现场观测冲突 | 生成协同事项，不自动改台账 |

导入来源需要关联原始文件、工作表、行号和列名，使系统未来能够回答一条数据来自哪一个 KDocs 导出文件、哪一个工作表和哪一行。

---

## 14. AI 自然语言查询

AI 查询采用“意图解析—权限校验—规则检索—结果解释”流程。

```text
用户问题
  ↓
AI 提取分支、IP、网段、设备、时间窗口和查询类型
  ↓
服务端校验用户角色与分支授权
  ↓
调用 IP/CIDR 规则引擎和数据库检索
  ↓
读取 AC、交换机、DHCP 等现场观测
  ↓
AI 生成有依据的自然语言解释
```

AI 不直接拼接 SQL，不直接调用交换机或 AC，也不直接执行任意扫描。主动诊断必须转化为受限任务，并经过权限确认、目标范围限制、频率限制和审计。

示例问题：“天津分支 192.168.1.254 现在有没有设备使用？”系统应解析为：分支=天津，IP=`192.168.1.254`，时间窗口=最近 10 分钟，查询=使用诊断。规则引擎再读取 AC 在线用户、AC IP/MAC、交换机 ARP/MAC/端口和台账记录，最终由 AI 解释证据和结论。

AI 结果必须包含命中记录、计算依据、数据时间、来源设备和不确定性。用户没有权限的分支和设备不能通过换一种问法查询到。

---

## 15. 协同与冲突处理

平台遇到台账与现场不一致时，应生成可分派的协同事项，而不是自动修改台账。

| 场景 | 系统行为 |
|---|---|
| 台账空闲，AC/交换机发现使用 | 创建疑似未登记占用事项 |
| 台账占用，现场长期无证据 | 创建闲置或设备离线复核事项 |
| 一个 IP 对应多个 MAC | 创建高优先级 IP 冲突 |
| 台账 MAC 与现场 MAC 不一致 | 创建资产替换或台账过期事项 |
| AC 在线但交换机无端口 | 标记数据源不一致，检查路径和采集范围 |
| 网段存在重叠 | 创建网络规划冲突事项 |
| 设备离线 | 保留上次数据，所有新判断标记为无法判断 |

事项支持责任人、优先级、状态、评论、附件、截止时间、处理结论和关闭原因。系统保留原始观测，不因关闭事项而删除证据。

---

## 16. 仪表盘设计

首页应展示决策信息和待处理事项，而不是单纯堆积数字。

| 区域 | 内容 |
|---|---|
| 核心指标 | 分支、网段、IPv4/IPv6、占用率、空闲量和设备在线率 |
| 分支概览 | 各分支网段数、使用率、空闲量和冲突数 |
| 网段分析 | 使用率最高/最低网段及空闲地址趋势 |
| 设备健康 | AC 和交换机在线、采集成功率、最近失败原因 |
| 待处理 | IP 冲突、重叠网段、导入错误和现场台账差异 |
| 协同动态 | 最近导入、分配、释放、修改、冲突处理和评论 |
| 快速操作 | 查询 IP、导入台账、登记设备、发起诊断 |

所有统计必须按当前用户授权的分支过滤，不能通过汇总数字泄露无权访问的数据。

---

## 17. 安全与网络前提

正式建设前需要完成企业网络与安全核查：采集器所在网段、交换机和 AC 管理地址、SNMP/NETCONF/RESTCONF/SSH/HTTPS 端口、中心平台出站地址、VPN/专线、防火墙策略、AC API 白名单和只读账号。

AC 文档示例使用 HTTP 端口 `9999`。如果目标设备支持 HTTPS，应优先使用 HTTPS；如果现场只能使用 HTTP，应将通信限制在隔离的设备管理网或安全隧道中，不能把管理接口直接暴露到公网。

主动探测应限制在 IPAM 已知且授权的网段和地址范围内，默认不执行全网 Ping 或端口扫描。所有探测任务记录发起人、目标范围、时间、结果和失败原因。

---

## 18. 分阶段实施路线

| 阶段 | 建设范围 | 主要验收结果 |
|---|---|---|
| 0. 现场核查 | 盘点台账、设备型号、版本、管理网、协议和 AC API | 形成设备能力和网络访问清单 |
| 1. IPAM 基础 | 分支、网段、IP、资产、权限、台账和基础审计 | 能替代 KDocs 完成集中查询 |
| 2. 导入治理 | Excel/CSV、字段映射、预检、冲突和来源追溯 | 能安全迁移长沙、天津台账 |
| 3. IP 规则引擎 | IPv4/IPv6、CIDR、掩码、范围、地址统计和归属 | 地址计算准确且可解释 |
| 4. 交换机采集 | SNMPv3、NETCONF/RESTCONF、SSH 只读兜底 | 获取 ARP、MAC、端口和 VLAN |
| 5. AC 适配 | 版本、健康、在线用户、IP/MAC、100 条限制处理 | 获取 AC 现场观测 |
| 6. 联合诊断 | 多来源证据合并、数据年龄、冲突和离线处理 | 区分使用、未发现和无法判断 |
| 7. 协同审计 | 差异事项、责任人、评论、前后差异和关闭原因 | 形成处理闭环 |
| 8. AI 查询 | 结构化意图、权限过滤、解释和主动诊断申请 | 自然语言查询可核验、不越权 |
| 9. 扩展适配 | 其他交换机、无线、DHCP、云平台 | 形成多厂商适配体系 |

---

## 19. 首期验收标准

首期平台应满足以下条件：可以建立多个分支并为用户配置不同分支权限；可以登记 IPv4、IPv6 和 CIDR 网段；可以计算掩码、范围、使用率和空闲数量；输入 `192.168.1.254` 可以识别其所属的 `192.168.0.0/16`；可以按 IP、网段、设备、负责人、部门和状态查询；可以导入 KDocs 导出的 Excel/CSV 并完成字段映射、错误反馈和冲突预检；能够登记交换机和深信服 AC；能够通过至少一种只读协议获取交换机网络信息；能够通过现场确认的 AC API 获取在线用户或 IP/MAC 信息；不会因设备离线将地址误判为空闲；可以展示诊断依据和数据时间；现场与台账不一致时生成待处理事项；所有导入、分配、释放、修改、设备接入、主动诊断和冲突处理都能审计；AI 只能返回当前用户有权限查看的结果。

---

## 20. 开发前必须补齐的信息

| 信息类别 | 需要提供的内容 |
|---|---|
| 分支 | 分支名称、编码、管理人员和网络范围 |
| 现有台账 | KDocs 导出的 Excel/CSV、工作表说明和字段含义 |
| 交换机 | 厂商、型号、软件版本、数量和管理地址范围 |
| 交换机协议 | SNMPv3、NETCONF、RESTCONF、SSH 的启用情况 |
| 交换机权限 | 只读账号可读取的 ARP、MAC、端口、VLAN 和 LLDP 能力 |
| 深信服 AC | 准确产品线、型号、软件版本和部署位置 |
| AC API | 开放接口开关、端口、白名单、接口权限和版本差异 |
| 网络连通性 | 采集器位置、路由、VPN、专线和防火墙策略 |
| 采集频率 | 实时、5 分钟、10 分钟或按需诊断 |
| 数据保留 | 原始观测、诊断结果、导入文件和审计日志保留周期 |

共享密钥、密码和 Token 不应通过普通聊天或文档传递。正式接入时应使用服务端安全配置和现场运维流程完成凭据录入。

---

## 21. 最终结论

本平台应建设为“中心 IPAM 管理平台 + 分支网络采集器 + 多厂商协议适配器 + 确定性 IP 规则引擎 + 权限约束 AI 助手”的体系。KDocs 导出的台账作为可追溯导入来源，数据库保存结构化事实，对象存储保存原始文件和校验结果；交换机和深信服 AC 提供现场观测，规则引擎负责判断地址归属和使用状态，AI 只负责理解问题和解释证据。

深信服 AC 接入应依据用户提供的接口文档实现：使用 AC 开放接口、请求方 IP 白名单、共享密钥与随机数 MD5 校验、GET/POST 参数规则、在线用户接口、IP/MAC 绑定接口和状态接口。考虑到本地请求限制、管理网隔离和接口单次最多返回 100 个在线用户，AC 适配器应优先部署在分支内网采集器中，并采用网段分片、能力探测、完整性标记和数据过期处理。

实施顺序应坚持“先数据模型和权限，再规则引擎和导入，再设备只读采集，再联合诊断，最后 AI”的原则。首期不自动修改交换机或 AC 配置，不因单次 Ping 结果判定 IP 空闲，不因设备离线删除现场证据，不允许 AI 绕过权限访问网络资源。这样才能在替代 KDocs 的同时，逐步形成可靠、可解释、可审计的企业网络资产治理能力。

---

## 参考资料

[1]: https://info.support.huawei.com/info-finder/encyclopedia/en/NETCONF.html "Huawei Technical Support — What Is NETCONF?"

[2]: https://support.huawei.com/enterprise/en/doc/EDOC1100278266/d73bfdce/overview-of-restconf "Huawei Enterprise Support — Overview of RESTCONF"

[3]: 用户提供附件《Sangfor_AC_API.pdf》，开放接口使用方法、Status 接口、IP/MAC 绑定接口、OnlineUsers 接口、错误信息及注意事项章节。
