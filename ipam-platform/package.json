{
  "name": "ipam-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "企业 IP 地址与网段资产管理平台（IPAM + 网络设备联动诊断 + 协同审计）",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "express": "^5.1.0"
  }
}
